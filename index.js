/**
 * @format
 */

import {AppRegistry} from 'react-native';
import BaseScreen from './app/screens/base';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => BaseScreen);
